@model Kundbolaget.Models.EntityModels.CustomerGroup

@{
    ViewBag.Title = "Kundgrupp Info";
}

<h2>Kundgrupp information</h2>

<div>
    <h4>Kundgrupp</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.CustomerList)
        </dt>


        @foreach (var item in Model.CustomerList)
        {
            <dd>
                @Html.DisplayFor(modelItem => item.Name)
            </dd>
        }
        </dl>
    </div>
<p>
    @Html.ActionLink("Ändra", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Tillbaka till listan", "Index")
</p>
