@model Kundbolaget.Models.EntityModels.Customer

@{
    ViewBag.Title = "Kund Info";
}

<h2>Information</h2>

<div>
    <h4>Kund</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Addresses)
        </dt>

        @* Loop through and render each category name *@
        <dd>
            @foreach (var address in Model.Addresses)
            {
                <dl class="dl-horizontal">
                    <dt>
                        @address.StreetName @address.Number
                    </dt>
                    <dt>
                        @address.PostalCode @address.Area
                    </dt>
                    <dt>
                        Type: @address.Type
                    </dt>
                </dl>
            }
        </dd>


    </dl>
</div>
<p>
    @Html.ActionLink("Ändra", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Tillbaka till listan", "Index")
</p>
